<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
      Main app theme: no action bar, solid white status bar with dark icons.
      Explicitly scopes DatePickerDialog and AlertDialog themes to clean
      Material defaults so dialogs never inherit Activity backgrounds.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">#FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:fitsSystemWindows">false</item>

        <!-- Explicitly scope DatePickerDialog & Dialog themes to clean Material defaults -->
        <item name="android:datePickerDialogTheme">@style/CustomDatePickerDialog</item>
        <item name="android:dialogTheme">@style/CustomAlertDialog</item>
        <item name="android:alertDialogTheme">@style/CustomAlertDialog</item>
    </style>

    <!-- Clean Material Date Picker Dialog Theme -->
    <style name="CustomDatePickerDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorAccent">#09090B</item>
        <item name="colorAccent">#09090B</item>
    </style>

    <!-- Clean Material Alert Dialog Theme -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorAccent">#09090B</item>
        <item name="colorAccent">#09090B</item>
    </style>

    <!--
      Launch / splash theme: shown before MainActivity.onCreate() runs.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#FFFFFF</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

</resources>